# Production Configuration (Docker Environment)
spring.application.name=AiHealthCoach

# Server Configuration
server.port=8080

# Database Configuration (Docker network)
spring.datasource.url=jdbc:mysql://mysql:3306/health_coach_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:healthcoach}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:healthcoach123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Logging - Production mode
logging.level.com.t2404e.aihealthcoach.config.RequestLoggingFilter=WARN
logging.level.root=INFO
spring.output.ansi.enabled=always

# JWT
app.jwt.secret=${APP_JWT_SECRET:my-super-secret-jwt-token-with-at-least-32-characters-long-and-then-some-more}
app.jwt.expiration-minutes=${APP_JWT_EXPIRATION_MINUTES:1440}

# OpenAI (Groq)
spring.ai.openai.api-key=${SPRING_AI_OPENAI_API_KEY}
spring.ai.openai.base-url=${SPRING_AI_OPENAI_BASE_URL:https://api.groq.com/openai}
spring.ai.openai.chat.options.model=meta-llama/llama-4-scout-17b-16e-instruct
spring.ai.openai.chat.options.temperature=0.6
spring.ai.openai.chat.options.max-tokens=8192

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dmnsx45tp}
cloudinary.api-key=${CLOUDINARY_API_KEY:143295335441532}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:49myn_EdcVFLG7q1oAUstvJpqds}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD:rsknrqqgozthebti}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Swagger & OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.enabled=true
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha
springdoc.default-produces-media-type=application/json
springdoc.default-consumes-media-type=application/json
